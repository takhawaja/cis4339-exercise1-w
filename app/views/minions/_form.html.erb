<%= form_with(model: minion, local: true) do |form| %>
  <% if minion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(minion.errors.count, "error") %> prohibited this minion from being saved:</h2>

      <ul>
        <% minion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :super_villain_id %>
    <%= form.text_field :super_villain_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
