<%= form_with(model: super_villain, local: true) do |form| %>
  <% if super_villain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(super_villain.errors.count, "error") %> prohibited this super_villain from being saved:</h2>

      <ul>
        <% super_villain.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
